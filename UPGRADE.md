# UPGRADE — обновление мозга агента

> Как обновиться и что сделать руками при переходе между версиями.
> История изменений — `CHANGELOG.md`. Текущая версия — файл `VERSION`.

## Механизм обновления (канон)

Канон описан в `.claude/agents/references/cold-start.md` (Шаг 5.5 и раздел
«обнови sysadmin»). Кратко:

- **Проверка новых версий:** хук `SessionStart` (`.claude/hooks/brain-update-check.sh`,
  ADR-0040) **раз в сутки** читает релизные теги `vX.Y.Z` прямо с удалённого
  (`git ls-remote`, репозиторий не меняется) и сравнивает с `VERSION`. Есть свежее —
  агент предложит «обнови sysadmin» одной строкой. Никогда не обновляется молча.
  Релизы берутся из remote `upstream`, если он есть, иначе из `origin`; явный выбор —
  `git config sysadmin.updateRemote <имя>`.
- **Применение:** по команде оператора «обнови sysadmin» агент выполняет:

  ```bash
  cd <sysadmin/>
  git fetch <remote-с-релизами> --tags
  git checkout --detach vX.Y.Z   # последний тег
  ```

  Detached HEAD — это контракт версионированной установки (дерево точно
  соответствует тегу), а не ошибка. **Исключение — своя копия мозга:** ты на ветке,
  и в ней есть твои коммиты, которых нет в релизе. Тогда агент не переключается на тег
  (это выключило бы твои коммиты), а сливает релиз в ветку — с брифингом и подтверждением.
  Конфликт при слиянии, свой коммит поверх отсоединённого тега или локальный тег с тем же
  именем, что у релиза, — агент останавливается и показывает, что нашёл, а не угадывает.
- **Перед обновлением** агент проверяет, что нет локальных правок
  (`git diff-index --quiet HEAD`) — иначе остановится и предложит варианты
  (ветка / откат / адресный коммит выбранных файлов).

## Таблица переходов: с какой версии → что сделать

| Ты на версии | Цель | Что сделать (помимо `git fetch --tags && git checkout --detach <тег>`) |
|---|---|---|
| 2.2.0 | 2.2.1 | **Патч, только правки поведения/доков — действий не требуется.** Уточнена зональная дисциплина (type-to-confirm не используется в Yellow), память приравнена к inventory по приоритету реальности (§4.6), `generate.py` OpenGate-подписки проставляет `LastUpdated`, рубрикатор `/retro` ловит хвосты «Попутно замечено». Данные в `infra/` не трогаются. Если используешь OpenGate-подписку — донеси обновлённый `generate.py` на боевой (бэкап + `scp` + один прогон sync), иначе крон пересоберёт из старого. |
| 2.1.x | 2.2.0 | **Минор, только продукт/доки — действий не требуется.** В `/sysadmin-meet` добавлена вводная Часть 0 (предзнания: git/SSH/jq/BotFather + «сервера ещё нет»); README честно описывает глубину; баг-репорт теперь через `scripts/collect-diagnostics.sh` + issue на github.com/vefmvai/sysadmin/issues. Данные в `infra/` не трогаются. |
| 2.0.x | 2.1.0 | **Минор, ничего ломающего — действий не требуется.** Полировка: новый скилл `/restore-from-backup`, переключение проектов в сессии через `SYSADMIN_PROJECT=<id>`, конкурентный лок `inventory-scan`, валидатор знаний `scripts/validate-knowledge.py` в pre-commit (нужен `python3`; нет — проверка пропускается с предупреждением). Данные в `infra/` не трогаются. |
| 1.11.x – 1.12.x | 2.0.0 | **Вход изменился (ADR-0015).** Ядро персоны переехало из `.claude/agents/sysadmin.md` в **`CLAUDE.md`**, а файл персоны стал тонкой заглушкой. Новый основной способ работы — **открыть папку `sysadmin/`** (`cd sysadmin && claude` или в IDE/desktop) и писать напрямую: персона грузится из `CLAUDE.md`, префикс `@sysadmin` нужен только из чужих папок. Перезапусти `/sysadmin-init` (или попроси агента) — он обновит выездной bridge `~/.claude/agents/sysadmin.md`, чтобы `@sysadmin` вёл на `CLAUDE.md`. Лимит ядра теперь в КБ с проверкой в pre-commit (ADR-0016). Скиллов стало 20 операционных + `/dev` (конструктор мозга). Ничего из твоих данных в `infra/` не трогается. |
| 1.4.x – 1.10.x | 2.0.0 | То же про вход 2.0 + конфиг с 1.10.0 расщеплён на два (ADR-0013): старый единый `sysadmin-config.json` ещё читается (legacy-fallback), но рекомендуется миграция — `/sysadmin-init` расщепит на `agent-config.json` (мозг) + `infra-config.json` (карта). Слой `_live/*.md` ушёл из git в 1.11.0 — при конфликте checkout: `git stash && git checkout --detach <тег> && git stash pop`. |
| ≤ 1.3.x | 2.0.0 | То же + активируй pre-commit hook: `git config core.hooksPath .githooks`. Проверь установку по `INSTALL.md` — состав скиллов, конфигов и способ входа с тех пор менялись существенно. |

## Как код отличает старый конфиг от нового

- Новые конфиги (`agent-config.json`, `infra-config.json`) несут поле
  **`version: "1.0"`** — версию ФОРМАТА файла (не версию мозга). Будущие
  несовместимые изменения формата поднимут это значение, и код будет узнавать
  старый файл по полю, а не по имени.
- Legacy-конфиг `sysadmin-config.json` (до расщепления) поля `version` не имеет
  и распознаётся по имени файла — поддерживается как fallback до миграции
  через `/sysadmin-init`.

## Если что-то пошло не так

- `git status` покажет, что мешает (локальные правки, конфликтные копии).
- Вернуться на предыдущий тег: `git checkout --detach v<прежняя-версия>`.
- Репозиторий в облачной папке (Яндекс.Диск и т.п.) при работе с двух машин
  может побиться — см. предупреждение в `cold-start.md` Шаг 4 (`git fsck`,
  перенос обмена на git remote).
