{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "g=\"${CLAUDE_PROJECT_DIR:-.}/.claude/hooks/brain-update-check.sh\"; if [ -f \"$g\" ]; then bash \"$g\"; else cat >/dev/null; fi",
            "timeout": 20,
            "statusMessage": "Проверяю, не вышла ли новая версия мозга…"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "g=\"${CLAUDE_PROJECT_DIR:-.}/.claude/hooks/red-zone-guard.sh\"; if [ -f \"$g\" ]; then bash \"$g\"; else cat >/dev/null; fi",
            "timeout": 15,
            "statusMessage": "Проверяю зону операции…"
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "g=\"${CLAUDE_PROJECT_DIR:-.}/.claude/hooks/retro-finding-gate.sh\"; if [ -f \"$g\" ]; then bash \"$g\"; else cat >/dev/null; fi",
            "timeout": 15,
            "statusMessage": "Проверяю основание находки…"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "g=\"${CLAUDE_PROJECT_DIR:-.}/.claude/hooks/inventory-sync-guard.sh\"; if [ -f \"$g\" ]; then bash \"$g\"; else cat >/dev/null; fi",
            "timeout": 20,
            "statusMessage": "Сверяю inventory с изменениями…"
          }
        ]
      }
    ]
  }
}
